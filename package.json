{
  "name": "tetrafi-docs-site",
  "private": true,
  "type": "module",
  "description": "Mintlify docs site. Tooling under scripts/ clones a source Mintlify site for a local working base (Step 1: fidelity-only). Never commit/push generated content.",
  "scripts": {
    "dev": "mint dev",
    "broken-links": "mint broken-links",
    "extract": "node scripts/recreate-from-source.mjs",
    "verify": "node scripts/verify-convergence.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "pixelmatch": "^6.0.0",
    "pngjs": "^7.0.0"
  }
}
