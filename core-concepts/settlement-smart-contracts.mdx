---
title: "Settlement & Smart Contracts"
description: "TetraFi's on-chain settlement architecture - escrow contracts, locks, and how trades are executed."
---

All TetraFi trades settle on-chain through the escrow settlement contracts. TetraFi never takes custody of user funds - the settlers verify signatures, enforce the signed order's terms, and move tokens delivery-versus-payment: the counterparty delivers, or your escrowed funds are refunded.

## The Contract Stack

TetraFi uses one escrow settlement system across both APIs, with an input side (where your funds lock) and an output side (where you receive):

| Contract              | Role                                                                          |
| --------------------- | ----------------------------------------------------------------------------- |
| `InputSettlerEscrow`  | Locks the taker's input funds under the signed order; releases on proof of delivery or refunds on expiry |
| `InputSettlerCompact` | Resource-lock variant of the input settler for pre-positioned balances        |
| `OutputSettlerSimple` | Pays out the order's output side on the destination chain                     |
| `HyperlaneOracle`     | Attests cross-chain delivery so the input settler can release escrowed funds  |

<Danger>
  Never send funds directly to a settler address. Funds move only through a signed order's lock - the quote response and preflight `nextActions` tell you exactly what to sign and where value flows.
</Danger>

### Per-Chain Deployments

Settler addresses differ per chain and are published in the deployment registry. For example, on Optimism (chain ID 10):

| Contract              | Address (Optimism)                           |
| --------------------- | -------------------------------------------- |
| `InputSettlerEscrow`  | `0x2778258002a69a0cB1DfD29b360a0bB1654C8652` |
| `InputSettlerCompact` | `0x7dfFC9e7db5a96887666c1a6D215E5d3Fb306d69` |
| `OutputSettlerSimple` | `0x2404F8e3c37c002c89bA78086a119e68E3fF8824` |

Always use the values carried in the quote response and preflight plan rather than hardcoding - the deployment registry is the source of truth. See [Supported Chains](/supported-chains) for the full list of networks.

## Settlement Lifecycle

When you request a quote, TetraFi returns an [EIP-712](/core-concepts/execution-modes) typed order payload (`escrow-v0`) representing the exact trade terms - assets, amounts, receiver, and expiry.

You sign this payload to authorize the trade. Your input funds lock in the escrow under those signed terms; the settler verifies the signature on-chain, and funds release to the counterparty only against proof that your output was delivered. On a cross-chain corridor, the oracle attests delivery on the destination chain before the source-side escrow releases.

If delivery cannot be proven before the order expires, the escrow refunds you - no partial state, no stuck funds, and TetraFi never takes custody of your tokens.

## Funding Locks

How your input funds enter escrow depends on the lock type available for the corridor and token:

| Lock type     | Description                                                          | Requires            |
| ------------- | -------------------------------------------------------------------- | ------------------- |
| Permit2       | Escrow pull authorized inside the signed message (no separate tx)    | One-time Permit2 approval |
| EIP-3009      | `transferWithAuthorization` for tokens that support it (e.g. USDC)   | No prior approval   |
| Resource lock | Pre-positioned balance in the compact settler                        | Prior deposit       |

The preflight `nextActions` select the lock for you - see [Token Approvals](/core-concepts/token-approvals) for the check-and-approve flow.

## Signed Payloads

Each quote carries its complete typed payload - domain, types, and message - in the `order` field:

| Order type  | What you sign                                                        |
| ----------- | -------------------------------------------------------------------- |
| `escrow-v0` | The `StandardOrder` typed data returned verbatim in `quote.order`    |

Sign exactly what the quote returns. The `integrityChecksum` binds your submission to the quote you were shown, so any mutation of the payload is rejected at submission.

See the [RFQ Order Submission guide](/rfq-api/guides/gasless-execution) and the [Router API quickstart](/router-api/quickstart) for the full signing flows.
